package org.jmeasure.core.signal.digital;

import org.jmeasure.core.signal.digital.BinarySignal;
import org.jmeasure.core.signal.sampler.Sampler;

/**
 * BinarySampler takes a binary signal and outputs a transformed binary signal
 */
public interface BinarySampler extends Sampler<Boolean, BinarySignal> {

	
}